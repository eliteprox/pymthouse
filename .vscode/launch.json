{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "pymthouse: Next dev (HTTPS)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --experimental-https --inspect",
      "cwd": "${workspaceFolder}"
    }
  ]
}
